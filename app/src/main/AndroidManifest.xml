<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ikna"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Ikna">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.DeckActivity"
            android:exported="false"
            android:label="Колода"
            android:theme="@style/Theme.Ikna"/>

        <activity
            android:name=".ui.AddEditCardActivity"
            android:exported="false"
            android:label="Добавить/Редактировать карточку"
            android:theme="@style/Theme.Ikna"/>

        <activity
            android:name=".ui.ReviewActivity"
            android:exported="false"
            android:label="Повторение"
            android:theme="@style/Theme.Ikna"/>

    </application>

</manifest>
